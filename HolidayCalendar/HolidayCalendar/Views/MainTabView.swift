import SwiftUI

/// Main tab navigation view
/// Rule 2.1: Tab ၄ ခု — Calendar → Todo → Memo → Settings
/// Placeholder — Step 2 မှာ implement လုပ်ပါမယ်
struct MainTabView: View {
    var body: some View {
        Text("Main Tab View")
    }
}
